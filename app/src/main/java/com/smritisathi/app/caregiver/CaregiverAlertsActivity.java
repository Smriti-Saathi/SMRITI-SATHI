package com.smritisathi.app.caregiver;import android.os.*;import android.widget.*;import com.smritisathi.app.core.*;public class CaregiverAlertsActivity extends BaseActivity {@Override public void onCreate(Bundle b){super.onCreate(b);header("Patient Alerts");root.addView(UI.card(this,"Monitoring Alert: Missed medicine reminder\n\nMonitoring Alert: Low activity today\n\nMonitoring Alert: Recent decline in game performance"));root.addView(UI.text(this,"These alerts are for monitoring only and are not a diagnosis."));}}
